This example application registers the protocol ```customscheme://```. 
When you type an URL like ```customscheme://coolvalue``` in a browser, the application will be opened,
and the URL displayed on the Window Title. 
